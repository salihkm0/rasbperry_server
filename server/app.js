// import express from "express";
// import cors from "cors";
// import videoRoutes from "./routes/videoRoutes.js";
// import { initializeDirectory } from "./utils/fileOperations.js";

// const app = express();

// app.use(express.json());
// app.use(cors());
// app.use("/api", videoRoutes);

// // Serve video files
// app.use("/videos", express.static(initializeDirectory()));

// export default app;


// import express from "express";
// import cors from "cors";
// import videoRoutes from "./routes/videoRoutes.js";
// import { initializeDirectory } from "./utils/fileOperations.js";

// const app = express();

// app.use(express.json());
// app.use(cors());
// app.use("/api", videoRoutes);

// // Initialize directory and serve video files
// const videosDirPath = initializeDirectory(); 
// app.use("/videos", express.static(videosDirPath));

// export default app;

